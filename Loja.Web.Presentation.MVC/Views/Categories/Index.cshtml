@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor Accessor

@{
    ViewData["Title"] = "Categories";
}

@section Scripts {

    <script src="~/js/Registration/Category/Index.js"></script>

}

    <head>
        <meta charset="utf-8" />
        <link rel="stylesheet" href="~/css/Global/00_Global.css" />
    </head>
    <body>
        
        <div class="register-wrapper">

            <!--- Modal --->
            <div id="div_Message" class="modal">

                <div class="modal-content">
                    <span class="close">&times;</span>
                    <p></p>
                </div>

            </div>
            <!--- Modal --->

            <div>
                @if (Accessor?.HttpContext?.Session.GetString("Role") == "Employee")
                {
                    <i class="fa fa-plus fa-2x" aria-hidden="true" onclick="window.location.href = '/Generics/Index?process=1&method=0';"></i>
                }
                
                <h1 class="register-title">@ViewData["Title"]</h1>
            </div>

            <div id="content"></div>

        </div>

    </body>